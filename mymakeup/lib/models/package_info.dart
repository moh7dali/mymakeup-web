class PackageAppInfo {
  String appName;
  String packageName;
  String version;
  String buildNumber;
  String buildSignature;

  PackageAppInfo(this.appName, this.packageName, this.version,
      this.buildNumber, this.buildSignature);


}
